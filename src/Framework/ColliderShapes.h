#pragma once

namespace Framework
{
	struct RectCollider
	{
		float width;
		float height;

		// 頂点データ

	};

	struct CircleCollider
	{
		float radius;
	};

	// 必要ならば、CircleCollider, BoxCollider, PolygonColliderなどを追加する
}